<!DOCTYPE html>
<html>
	<head>
    <title>
        Editar Usuario
    </title>
    <link th:href="@{../../css/inicio.css}" rel="stylesheet" type="text/css" media="screen"/>
	</head>
<body>
<div class="heading">
    <ul class="menu">
        <li><h1>PIZZA<span>RAP</span></h1></li>
    </ul>
</div>
<div class="container border gx-5">
    <h1 >Editar Usuario</h1>
    <form action="#" th:action="@{/usuarios/admin/editar}" th:object="${usuario}" method="post" role="form">
    <div class="container">
		<input type="hidden" th:value="${usuario.id}" th:attr="name='id'"/>

        <label for="email"><b>Nombre</b></label>
        <input type="text" th:field="*{nombre}" placeholder="Ingrese su nombre" name="email" id="email" required>

        <label for="psw"><b>Apellido</b></label>
        <input type="text" th:field="*{apellido}" placeholder="Ingrese su Apellido" name="psw" id="psw" required>

		<button type="submit" class="registerbtn">Editar</button>
		
		<a href="/usuarios/admin/inicio" class="button">Volver al inicio</a>
    </div>
</form>
</div>

</body>
</html>